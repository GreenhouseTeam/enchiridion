package dev.greenhouseteam.enchiridion.client.util;

import net.minecraft.client.gui.GuiGraphics;
import net.minecraft.core.Holder;
import net.minecraft.world.item.trading.MerchantOffers;

import java.util.List;

public class EnchantingTableScreenUtil {

    // TODO: Implement scroll bar for the enchanting screen.
    /*
    private void renderScroller(GuiGraphics graphics, int x, int y, int scrollIndex, int enchantmentSize) {
        int laterEnchantmentsAmount = enchantmentSize + 1 - 3;
        if (laterEnchantmentsAmount > 1) {
            int $$5 = 139 - (27 + ($$4 - 1) * 139 / $$4);
            int $$6 = 1 + $$5 / $$4 + 139 / $$4;
            int $$8 = Math.min(113, scrollAmount * $$6);
            if (this.scrollOff == $$4 - 1) {
                $$8 = 113;
            }

            graphics.blitSprite(SCROLLER_SPRITE, $$1 + 94, $$2 + 18 + $$8, 0, 6, 27);
        }
    }
     */
}
